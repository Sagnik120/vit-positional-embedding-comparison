name: vit-pe-comparison
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch>=2.2.0
      - torchvision>=0.17.0
      - einops>=0.7.0
      - numpy>=1.24.0
      - pandas>=2.0.0
      - matplotlib>=3.7.0
      - tqdm>=4.65.0
# Optional alternative to the venv + requirements.txt workflow in README.md.
# Use with: conda env create -f env/environment.yml && conda activate vit-pe-comparison
